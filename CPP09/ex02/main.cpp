#include "RPN.hpp"

int main(int argc, char** argv)
{
	if (argc != 2)
	{
		std::cout << "Error: the program requires an argument: <whole numbers separated by spaces>" << endl;
		return (1);
	}

	

	return (0);
}